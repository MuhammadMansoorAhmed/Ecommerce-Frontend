// eslint-disable-next-line react/prop-types
const Layout = ({ children }) => {
  return (
    <div className="d-flex justify-content-center w-100 h-100">{children}</div>
  );
};

export default Layout;
